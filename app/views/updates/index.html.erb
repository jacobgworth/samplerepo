<div id="wrapper">
<%=render 'layouts/navBar'%>
<div style="padding:50px 0px 0px 0px;font-size: 25px;">
<% if @cid.nil?%>
	<h1>Mission of Hope Updates</h1>
<% else %>
	<h1><%=@category.categoryname %> Updates</h1>
<% end %>
</div>


<% @updates.each do |update| %>
  <div style=padding-top:20px;>
  <h2><%= update.created_at.strftime("%b %e, %Y") %> - <%= update.title %></h2>
    <td><%= update.body %></td>
    <td><%= update.media %></td>
    <!--<td><%= link_to 'Show', update %></td>
    <td><%= link_to 'Edit', edit_update_path(update) %></td>
    <td><%= link_to 'Destroy', update, confirm: 'Are you sure?', method: :delete %></td>-->
  </tr>
  </div>
<% end %>

<br />

<%= link_to 'New Update', new_update_path %>
