<%= form_for(@category) do |f| %>
  <% if @category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
   <p>Category Name</p><br />
    <%= f.text_field :categoryname %>
  </div>
  <div class="field">
    <p>Category Description</p><br />
    <%= f.text_field :categorydesc %>
  </div>
  <div class="field">
   <p>Category Icon</p><br />
    <%= f.text_field :categoryicon %>
  </div>
  <div class="field">
   <p>Category Hero</p><br />
    <%= f.text_field :categoryhero %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
